---
const { key, value } = Astro.props
const hasContetn = key && value
---
{
  hasContetn && (
    <section class="flex gap-1">
      <span class="whitespace-nowrap">{key}:</span>
      <p>{value}</p>
    </section>
  )
}